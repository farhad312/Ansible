- name: service all host
  hosts: myservers
  gather_facts: no
  tasks:
    - name: give list host
      service_facts:

    - name: sort lists
      debug:
        msg: "{{ item.key }} --> {{ item.value.state }}"
      loop: "{{ ansible_facts.services | dict2items | sort(attribute='key') }}"

